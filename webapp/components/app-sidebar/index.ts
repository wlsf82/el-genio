import { AppSidebarMenuItem } from './menu-item'
import { MenuTitle } from './menu-title'
import { AppSidebarRoot } from './root'

export const AppSidebar = {
  Root: AppSidebarRoot,
  MenuItem: AppSidebarMenuItem,
  MenuTitle: MenuTitle,
}
